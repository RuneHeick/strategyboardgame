<Window x:Class="Coordinator.View.ProjektionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:oxy="http://oxyplot.org/wpf"
        Title="ProjektionView" Height="500" Width="500">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Grid.ColumnSpan="2" Text="{Binding CurrentTeam, FallbackValue=Team}" FontSize="40" HorizontalAlignment="Center"/>
        <ContentControl Grid.Column="1" Grid.Row="1" Content="{Binding SelectedView}" />
        <ItemsControl  Grid.RowSpan="2" ItemsSource="{Binding Teams, Mode=OneWay}" >
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Height="60" Margin="5" Background="Green" >
                        <TextBlock Text="{Binding Mode=OneWay}" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Center" />
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <oxy:Plot Grid.Row="3" Grid.ColumnSpan="2" Model="{Binding Model}" />
        
    </Grid>
</Window>
