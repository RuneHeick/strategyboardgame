<Window x:Class="Coordinator.View.ProjektionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:oxy="http://oxyplot.org/wpf"
        Title="ProjektionView" Height="500" Width="500">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Grid.ColumnSpan="2" Text="{Binding Name, FallbackValue=Team}" FontSize="25" HorizontalAlignment="Center"/>
        <ContentControl Grid.Column="1" Content="{Binding Item}" />
        
        <oxy:Plot Grid.Row="2" Grid.ColumnSpan="2" Model="{Binding Model}" />
        
    </Grid>
</Window>
